apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-prod
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  project: default
  sources:
    - repoURL: https://helm.releases.hashicorp.com
      chart: vault
      targetRevision: "0.30.1"
      helm:
        valueFiles:
          - $values/vault-eso/vault/values/base.yaml
          - $values/vault-eso/vault/values/prod.yaml
    - repoURL: https://github.com/iahmad-khan/infrastructure-as-code
      targetRevision: HEAD
      ref: values
  destination:
    name: prod
    namespace: vault
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
